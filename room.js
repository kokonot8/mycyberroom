const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));
const meta={tools:['⌘','tools','small tools, utilities and useful experiments'],music:['♫','music','tracks, sketches and sound experiments'],engproject:['⌁','engproject','engineering projects and technical things'],diary:['↳','diary','recent thoughts, long notes, short notes, whatever needs somewhere to live'],poet:['✎','poet','words, fragments and poems I want to keep'], 'visual-memory':['▣','visual memory','images and moments I want to remember'],misc:['※','杂','things that refuse to fit anywhere else'],random:['?','random area','a room with no job description']};
(async()=>{const root=document.querySelector('#room');const key=new URLSearchParams(location.search).get('room')||'misc';const m=meta[key]||meta.misc;document.title=`${m[1]} — my hallway`;try{const r=await fetch('data/content.json');if(!r.ok)throw new Error(r.status);const data=await r.json(),items=data[key]||[];root.innerHTML=`<header class="room-hero"><span class="big-icon">${m[0]}</span><h1>${esc(m[1])}</h1><p>${esc(m[2])}</p></header><section class="entries">${items.length?items.map((it,i)=>`<article class="entry" id="entry-${i}"><div class="entry-top"><h2>${esc(it.title)}</h2><span class="date">${esc(it.date)}</span></div>${it.image?`<img src="assets/${encodeURIComponent(it.image)}" alt="${esc(it.title)}">`:''}${it.text?`<p>${esc(it.text).replace(/\n/g,'<br>')}</p>`:''}${it.link?`<a href="${esc(it.link)}" target="_blank" rel="noopener">open →</a>`:''}</article>`).join(''):`<div class="empty">nothing here yet. come back later.</div>`}</section>`}catch(e){root.innerHTML='<div class="error">Could not load this room. Preview through a web server or GitHub Pages.</div>'}})();
